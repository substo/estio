#!/bin/bash

set -e

echo "deploy-direct.sh is retired and must not be used for production deploys."
echo "Use ./deploy-local-build.sh as the single supported deploy path."
echo "Evolution API is retired and is not deployed by Estio deploy scripts."
exit 1
