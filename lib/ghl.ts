import { GHL_CONFIG } from "@/config/ghl";

// Custom Menu Link API is not available on Standard plan ($97/month)
// Users must manually add the app URL to their GHL dashboard or sidebar
// URL format: ${APP_BASE_URL}/sso?locationId={{location.id}}
